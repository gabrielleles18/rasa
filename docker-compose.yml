services:
  rasa:
    env_file:
      - .env
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5005:5005"
    volumes:
      - ./:/app
    environment:
      - RASA_LICENSE=${RASA_LICENSE}
    command: run --enable-api --cors "*"
